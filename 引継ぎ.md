# みわダッシュボード v.3 — 引継ぎメモ（2026-06-17）

新チャットはこのファイルを最初に読んでから作業開始すること。プロジェクトの規約は `CLAUDE.md`（特にシフトページ／CSV取込）も併せて参照。

---

## 現在の大目標

**業務メニューの全ページを「PC・モバイル3段階完成」**

→ **✅ v3.00 リリース完了。FAQ含む全機能が実装済み。**

---

## 現在のバージョン番号

**最新使用済み：v=263**  
次に何かファイルを編集したら **v=264 以上** を使うこと。

---

## 全メニュー進捗

| メニュー | target | ファイル（最新v） | 状態 |
|---|---|---|---|
| クレーム・事故品 | claims | polished-mobile-claims.jsx (v=210) | ✅ 完成 |
| ありがとうカード | thanks | polished-mobile-thanks.jsx (v=210) | ✅ 完成 |
| 共有ボード | board | polished-mobile-board.jsx (v=232) | ✅ 完成 |
| 料金表 | price | polished-mobile-price.jsx (v=227) | ✅ 完成 |
| FAQ管理 | faq | polished-mobile-faq.jsx (v=263) | ✅ **3段階目完成** |
| 売上レポート | sales | polished-mobile-sales.jsx (v=235) | ✅ 完成 |
| シミ抜き報告 | stain | polished-mobile-stain.jsx (v=233) | ✅ 完成 |
| 請求書管理 | invoice | polished-mobile-invoice.jsx (v=239) | ✅ 完成 |
| 工場報告 | factory | polished-mobile-factory.jsx (v=261) | ✅ 完成 |
| フィードバック | feedback | polished-mobile-feedback.jsx (v=248) | ✅ 完成 |
| 車両管理 | vehicle | polished-mobile-vehicle.jsx (v=243) | ✅ 完成 |
| アカウント・その他 | about | polished-mobile-about.jsx (v=224) | ✅ 完成 |
| AIレポート | — | — | ✅ **PC限定・業務メニューから削除済み** |

---

## 次にやること

- 機能説明書を v3.00 に更新（FAQ・PDFビューア・モバイル3段階目の解説を追加）
- ダッシュボードKPIの実データ反映（現在一部サンプル値）

---

## FAQシステム構成（v3.00 完成版）

### ファイル構成
| ファイル | 役割 |
|---|---|
| `formsite/FAQ.html` | スタッフ向けチャット。AI回答・知識ベース参照・料金表参照・PDF/画像添付ビューア（ピンチズーム対応） |
| `faq-admin.js` | PC版FAQ管理ロジック。知識CRUD・未回答・資料ストック・GAS連携・ファイルアップロード |
| `faq-admin.css` | PC版スタイル |
| `FAQ管理.html` | PC版FAQ管理ページ |
| `polished-faq-admin.jsx` | ダッシュボードシェル内にfaq-admin.jsをマウント |
| `polished-mobile-faq.jsx` | モバイル版FAQ（KPI・未回答回答入力・質問ログ・知識ベース検索） |
| `gas/faq-knowledge-base.gs` | FAQ専用GAS。KB・未回答・FAQログ・ファイルアップロード |

### GAS URL
| 用途 | URL |
|---|---|
| **AI呼び出し**（Claude Haiku プロキシ） | `https://script.google.com/macros/s/AKfycbwgjgqVJNFNnNwNyzc8DsskESrfvoSSTgpK6T2twFPTVyDrhnR2NhNy_CLiajfB1pC_OA/exec` |
| **知識ベース・ログ共有** | `https://script.google.com/macros/s/AKfycbzWq4dsfENPZuZ9eGGum5Glg2pDcLf10bL8dJNvJgr66cgUOHAFGWPJNmkRUl3CpAml/exec` |

### formsite/FAQ.html の注意点
- 料金データは `./price_seed.js`（同じformsiteフォルダ内）から読込（`../` ではない）
- PDF表示はPDF.js（CDN）でピンチズーム対応のアプリ内ビューア
- GAS URLはlocalStorage未設定でも DEFAULT_KB_GAS でデフォルト動作

---

## シフトページ

- **タブ追加**：「今日の出勤」と「月間スケジュール」の2タブ構成（polished-mobile-shift.jsx v=250）
- 月間スケジュールは**横スクロール表形式**（名前列固定・日付が右スクロール）

---

## ⚠️ キャッシュ運用（最重要）

ファイルを編集したら、読み込み側HTMLの `?v=NNN` を必ず上げる。番号の使い回し厳禁。

- **現在の最新：v=263**。次は **v=264 以上**。
- `モバイル.html` 内で各 jsx/seed に `?v=` が付いている。編集したファイルすべてに新番号を付ける。
- `polished-mobile.jsx`（シェル）は **v=240** のまま。中身を変更した場合は番号を上げること。

---

## ⚠️ deploy/ フォルダは廃止済み

v3.00 より deploy/ フォルダを廃止。**root のファイルを直接 GitHub Desktop でプッシュ**する運用に変更。

---

## モバイル画面を1つ作るときの手順（テンプレ）

1. **PC版のデータ層を調べる**：対象HTMLが読む seed / localStorage キー / cloudGet シート名を把握。
2. **`polished-mobile-XXX.jsx` を新規作成**。末尾で `window.MXxx = MXxx;` を export。
   - props: `registerHeader({title, sub})` と `registerFab(fn|null)` を受け取る。
   - 関数・定数はファイル固有の接頭辞を付ける（例 `ms`/`MS_`）。**`styles` 汎用名禁止**（Babel複数ファイルは同一スコープ）。
3. **CSS は `polished-mobile.css` に追記**（接頭辞付きクラス）。
4. **配線（3ファイル）**：
   - `polished-mobile-menu.jsx`：該当項目を `kind:"view", target:"xxx"` に変更。
   - `polished-mobile.jsx`：`M_SUBVIEWS`／`M_TAB_TITLE`／`M_PARENT_TAB` の3定数に追加。
   - `モバイル.html`：`<script type="text/babel" src="polished-mobile-xxx.jsx?v=NNN">` を追加。
5. **テスト**：`scraps/xxx-test.html` で単体描画確認→削除。
6. **バージョン上げ**：編集した全ファイルに新番号。

---

## データ層メモ

- **シフト**：seed = `polished-shift-seed.js`（`window.SHIFT_2026_06`）。
- **料金表**：seed = `price_seed.js`(緑) / `price_seed_blue.js`(青)。formsite内には `formsite/price_seed.js` として別途配置。
- **売上**：seed = `polished-sales-seed.js`（`window.MIWA_SEED_DATA`）。localStorage = `miwa.sales.v9`。
- **請求書**：データ層 = `polished-invoices-data.jsx`（GAS直接フェッチ）。localStorage = `miwa.invoices.v1`。
- **フィードバック**：GAS直接フェッチ。localStorage = `miwa.feedback.v3`。
- **シミ抜き**：GAS直接フェッチ。localStorage = `miwa.stain.v1`（PC版と共有）。
- **車両**：データ層 = `polished-vehicles.jsx`。localStorage = `miwa.vehicles.v2`。
- **FAQ KB**：GAS URL = `miwa.faq.cloud.v1`（gasUrl/token/enabled）。デフォルトURL内蔵。
- **工場報告**：GAS直接フェッチ。localStorage = `miwa.factory.v3`。
- **共有ボード**：localStorage = `miwa.board.v1`、クラウドシート = `"共有事項"`。
- **クラウド**：`polished-cloud.jsx` の `cloudEnabled()` / `cloudGet(sheet)` / `cloudAdd(sheet,row)`。

---

## モバイル.html の主要スクリプト構成

```
auth-config.js / polished-auth.js / polished-device.js
polished.css / polished-mobile.css (v=250)

共有データ層: polished-shift-seed.js / price_seed.js / price_seed_blue.js
              function_seed.js / sales_seed.js / polished-sales-seed.js
              polished-cloud.jsx / polished-atoms.jsx / polished-claims-data.jsx

モバイル各タブ（v番号は各自参照）:
  home / menu / shift(v=250) / claims / thanks / price / board
  sales / stain / invoices-data + invoice(v=239) / faq(v=263) / factory(v=261)
  feedback(v=248) / vehicles + vehicle / news / about

アプリシェル: polished-mobile.jsx (v=240)
```
